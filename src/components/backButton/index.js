import {Button} from "antd";
import React from "react";

const BackButton = () => {
    return (
        <div style={{ flex: 1 }}>
        <Button>
            Go Back
        </Button>
        </div>
    )
};

export default BackButton;